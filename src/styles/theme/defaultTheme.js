import { media } from '../abstract/breakpoints/extendedBreakpoints';
import { colors } from '../abstract/variables/colors';
import { fonts } from '../abstract/variables/fonts';
import { transitions } from '../abstract/variables/transitions';
import { animations } from '../abstract/variables/animations';
import { zIndex } from '../abstract/variables/zIndex';
import { sizes } from '../abstract/variables/sizes';

export const defaultTheme = {
	media,
	colors,
	fonts,
	animations,
	transitions,
	sizes,
	zIndex,
};
